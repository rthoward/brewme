
<div class="siblings">
  <h4>
    other recipes that modify <%= link_to @recipe.parent.name, @recipe.parent %>
  </h4>

  <ul>
    <% @recipe.siblings_only.each do |sibling| %>
      <li><%= link_to sibling.name, sibling %></li>
    <% end %>
  </ul>

</div>